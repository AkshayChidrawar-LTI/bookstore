# The main pipeline for raw_to_bronze

resources:
  pipelines:
    raw_to_bronze_etl:
      name: raw_to_bronze_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "."

      libraries:
        - glob:
            include: transformations/**
